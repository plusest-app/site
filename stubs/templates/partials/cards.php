<?php
/**
 * ============================================================================
 *  Сітка карточок обʼєктів
 * ============================================================================
 *
 *  Три картки в рядок на великих екранах, дві на середніх, одна на малих —
 *  межі задані в site.css (клас .pl-grid).
 *
 *  Файл використовується у трьох місцях, тому й винесений окремо:
 *
 *    * список обʼєктів;
 *    * блок «Схожі обʼєкти» на сторінці публікації;
 *    * розділ «Обране» — там цей шаблон рендериться сам собою і віддається
 *      скрипту як відповідь на запит '/base/favorites/?ids=...'.
 *
 *  Доступні змінні:
 *
 *    $objects — обʼєкти, які показуємо
 *    $found   — (лише для обраного) ідентифікатори, які сервер знайшов.
 *               Скрипт порівняє їх зі своїм списком і прибере зі сховища
 *               обʼєкти, яких більше немає.
 * ============================================================================
 */

$found = isset($found) ? $found : null;
?>
<?php if ($found !== null): ?>
    <?php /* Службова позначка для site.js. Ніяк не показується. */ ?>
    <span data-pl-found="<?= $view->esc(implode(',', $found)) ?>" hidden></span>
<?php endif ?>

<div class="pl-grid">
    <?php foreach ($objects as $object): ?>
        <?= $view->partial('partials/card', ['object' => $object]) ?>
    <?php endforeach ?>
</div>
